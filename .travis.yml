language: cpp

services:
  - docker

before_install:
  - docker pull asneg/asneg-demo

script:
  - val="$(docker run -d -t asneg/asneg-demo)"
  - echo $val
  - docker ps
  - docker exec $val /tmp/ftest/test.sh
